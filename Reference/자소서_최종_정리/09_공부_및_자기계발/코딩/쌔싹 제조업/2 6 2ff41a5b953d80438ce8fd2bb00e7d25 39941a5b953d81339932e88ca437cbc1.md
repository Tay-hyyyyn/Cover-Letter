# 2 6 2ff41a5b953d80438ce8fd2bb00e7d25

# 2/6

AIC BIC는 낮을수록 좋고 adj R^2는 높을 수록 좋다.

로지스틱 회귀의 개요

로짓변환 : 무한대로 생긴 놈을 0~1까지의 값으로 변환한다.

로짓은 ln(p/1-p) : B0 + B1X1 + … + BpXp

범주형 데이터가 한쪽으로 치우친 데이터는 불균형데이터(imbalanced data)

- 다수 범주를 더 잘 맞추려고 한다. 하지만 우리가 궁금한건 소수 범주인 경우가 많다.
- up-sampling or down-sampling으로 임의적으로 맞춰주기도 하나 성능 낮음 때문에 가중치를 주는게 적절

dodge : 펼친 그래프

stack : 쌓은 그래프

y = B0 + B1R2 + … +

오즈(승산)

오즈에 로그를 싀운 것 = 로

로짓은 머신러닝 분류모델에서 중요한 역할을 한다.

로짓을 알면 확률을 계산할 수 있다.

sigmoid 함수 : 1/1+e^-logit

주어진 모수에서 표본이 나타날 확률 밀도가 최대가 되는

모수가 주어졌을때 x라는 입력값이 있을때 출력 값을 예측하는 것.

얼마나 합격에 영향을 미치는 가

log(P^yi

회귀계수를 자연상수가 밑인 지수변환을 하면 오즈비를 얻을 수 있습니다.

오즈비는 해당변수가 +1 증가할때 합격 오즈가 몇배인지 나타낸 값입니다.

합격 오즈를 알면 확률을 얻을 수 있습니다.