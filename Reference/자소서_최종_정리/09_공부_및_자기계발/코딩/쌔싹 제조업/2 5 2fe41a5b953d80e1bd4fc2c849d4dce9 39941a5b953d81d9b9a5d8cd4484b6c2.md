# 2 5 2fe41a5b953d80e1bd4fc2c849d4dce9

# 2/5

0.8이상의

정규성 만족 : 값으로 판단 > 평균사용 가능

정규성 불만족 : 순위로 판단 > 중위수 사용가능

단측검정과 양측검정이 둘다 있다.

표준정규분포를 따른다.

요인이 3개 이상일 시 분산분석은 ttest여러번 실행한 결과와 다르다.

집단 간 변동은 요인으로 설명가능

집단 내 변동은 요인으로 설명불가

각 집단에서 평균을 뺀 설명 불가능한 부분이 0이 되던가

ttest의 경우 윌 콕슨 순위합검정

크루스칼 왈리스 순위합 검

사후분석(다중비교) - 시험에 나옴

tukey - 집단 갯수가 6개 이상일때

scheffe - 사회과학에서 많이 쓴다.

dunnett - 실험군 갯수가 많을때

tamhane - 등분산을 가정하지 않아도 사용가능

nemenyi - 순위 데이터에 사용

교차분석

교차분석 표 - Cross

피어슨 상관분석 CC

독립표본 t검정 Automatic

분산분석 : CNG 제거하고 t검정 유의

R^2 결정계수는 두 변수의 상관계수의 제곱이다.

입력변수의 갯수에 따라 단순선형회귀와 다중선형회귀로 구분한다.

선형회귀모형에서 목표변수의 변동을 설명할 수 있는 큭를 결정계수라 한다.

선형회귀의 기본 가정

목표변수와 입력변수는 상관관계를 가져야한다.

변수선택법 - Step

Ridge, Lasso - B베타를 0으로 강제

결정트리 - 변수를 뺀다.

입력변수끼리는 강한 상관관계가 없어야한다.

다중공선성 변수를 제거하면

예측력이 낮아진다. > 설명을 위한 작업이지 예측력을 위한 작업은 아니다.(머신러닝에 부적합)

한 시점의 모든 칼럼의 값은 횡단면분석(cross sectional)

모든 시점에 한 칼럼의 모든 값은 시계열(time-series)

선형 회귀직선은 입력변수의 전체 영역에서 목표변수와의 관계를

선형회귀 :

여러개 인과관계 확인

1. Y-X 상관관계
2. X-X 상관관계 크지 않아야한다. >다중공선성 다른 x에 종속되는 x변수를제거
3. 잔차 가정 - 정규성, 등분산성, 독립성
4. 이상치 제거 - 전체 모형을 망가뜨리므로 이상치를 제거한다.

다중 선형 회귀 모형의 시각적 이해

빨간점과 파란점 사이에 평면을 확인합니다.

회귀계수 추정

OLS - 최소제곱법

유의성 검정 - 모형 : 최소한개는 0이 아니다. 계수 : 누가 0이 아닌가

회귀진단 - 그래프 진단, 검정진행

다중공선성 해결

범주형 > 수치형으로 바꿔야한다.

명목형 일때는 원핫 인코딩 수행, 더미 변수 생성

서열형 일때는 오디널 인코딩 수행, 순서부여

형태 둘다 상관없이 타겟 인코딩 사용 가능

차원의 저주 -

원핫 인코딩 - 더미변수 부여

오디널 인코딩 - 임의로 순서를 부여한다

타겟 인코딩 - 부여받은 y값으로 계산을 한다.

데이터 리키지(미래의 데이터를 예측한다) 문제가 발생

SST전체분산 = SSR + SSE

MSE

MSR

y의 절편은 B0이고 이는 평균이다.

회귀 모형의 유의성 검정 -

F값의 유의확률이 첫번째로 고려해야할 사안

0.05보다 작으면 귀무가설 기각가능

0.05보다 크면 근거가 부족하다 기각불가능이 아니다

관측값을 늘리거나 적절한 변수를 추가하는 전처리 과정 필요

회귀 계수의 유의성 검정 -

회귀계수가 0이 아니어야한다.

선형 회귀 모형 결과 표에서 t값의 유의확률이 0.05보다 작으면 귀무가설 기각

잘못된 머신러닝에서는 R^2가 음수가 나온다.

결정계수는 입력변수와 목표변수가 강한 상관관계를 가질 수록 따라서 크게 증가한다.

R^2 : 적합된 모형의 설명력을 위한 것(모형 선택 기준은 아니다)

adj R^2는 y와 관계도가 높은 변수만 받기위한 (모형 선택 기준으로 사용)

머신러닝에서는 AIC, BIC를 사용해서 모형선택기준으로 사용

Cook’s D (쿡의 거리)

잔차가 전체 데이터에 영향력이 큰 정도

선형 회귀 분석 : Y와의 관계를 설정. 크기 확인 > 설명목적

1 X-Y 상관관계 존재

2 X-X 상관관계 존재 x > 다중공선성 > 설명력나눔 > 해석곤란

머신러닝 > 예측력을 위해서 다중공선성이 있어도 변수를 제거하면 예측력이 줄어서 그냥 다중공선성 감안한다.

3 잔차가정 : 정규성가정(그림으로 확인시 nomal q-q, KDEplot, shapiro test), 등분산성 가정, 독립성 가정

회귀계수 추정 : 최소제곱법(OLS)

변수가 증가하면 항상 R^2(결정계수)가 증가한다

조정된 결정계수는 Y와 관계없는 변수 추가 하지 말아라 그렇기 때문에 최종모형 선택기준이 된다.