# 2 13 30641a5b953d809cb75af2142d2f6934

# 2/13

결정트리는 모델이 복잡해질수록(leaf수가 많다>관측값 개수 감소) 과접합이 자주 발생한다.

결정트리는 불순도를 가장 크게 낮출 수 있는 분리 기준점(특성과 임계값)을 찾아서 나무를 성장시킨다

특징 : 비모수적, 이상치 둔감

이해하기 쉽기 때문에 활용하기 쉽다.

단점 : 과적합 하기 쉽다.(모델을 단순화해야한다.)

해결법 : 정지규칙(사전가치치기Stopping rules) 부모 노드의 관측 값 갯수의 하한을 설정, 자식노드의 관측값도 설정,

최대깊이(10정도)

```
    가지치기(사후가지치기pruning) 비용 복잡도 = R(T) + A|T|    A = 비용복잡도 파라미터
```

ccp_alphas는 증가

leafes 감소

depth감소

training accuracy 감소

validation accu 감소

1. 실제 값 상대도수를 cut-off로 설정 그리드서치 > 0~1.0
2. 검증셋 정확도 대신 F1 점수 기준으로 가지치기
3. SMOTE를 활용한 데이터 균형화
4. class=weight

**앙상블**

분류 방법 5 :

로짓

릿지

리쏘

KNN

Tree

결정나무 : 분류(리프 노드에서 타겟의 다수결)와 회귀(리프 노드에서 타겟의 편균)가 있다.

랜덤포레스트는 독립적인 나무들을 사용하여 과적합을 줄여준다.

특성이 n개 있으면 분류는 루트n, 회귀의 경우 n/3

랜덤 포레스트는 각각의 특성의 성질을 잘 뽑아 낼 수 있다.

배깅은 독립적으로, 부스팅은 순차적으로

부스팅

약한 학습기 : weak learner

강한 분류기로 만들어야한다.

과적합 발생이 쉽다. 때문에 early stopping이 있다.

일반적으로 랜덤 포레스트(집단지성) 보다 부스팅이 좋다.

랜덤포레스트 부스팅 그레이디언트 앙상블 해서 섞어서 만든다.

그레이디언트 부스팅은 과적합을 막지만 속도가 오래걸린다. > Xgboost는 그나마 빠르다.

Instance = Data Point = Obserbation

결정트리 : 하나의 나무 모델 학습

랜덤 포레스트 : 독립적인 개별 나무 모델을 여러개 학습하고 결과를 결합

데이터 표준화를 할 필요가 없다.

선형회귀 / 로지스틱 회귀 : 경사하강법 수렴에 빠르게 도달하기 위해서(선택)

최근접 이웃 : 거리를 이용하기 때문에(필수)

주성분 분석 : 필수는 아님, 주성분의 분산 비율을 이해하기 좋다.(선택)

군집 분석 : 거리를 이용하기 때문에(필수)

데이터 표준화를 하면 규칙을 이해하기 쉽다.

이상치에 대해 민감하지 않다.

결측값은 처리해야 한다.

랜덤 포레스트 : 부트스트랩 샘플 사용 > 선택되는 행은 서브셋 학습, 되지 않는 행은 OOB 검증

OOB 시각화 하는 이유는 개별 나무 모델을 어느정도 학습하면 성능이 안정화되는지 확인용.

그리드 서치 : 트리개수, 특성개수 모든 가능한 경우의 수에서 사용

랜덤 서치 : 시도 횟수를 정해준다.

랜덤 포레스트 특징 :

1. 부트 스트랩 서브셋(행, 열 복원추출)을 사용하여 과적합 문제 완화
2. 집단 지성 : 독립적인 개별 나무(과적합이 된 나무)를 여러개 (정지규칙을 완화하고 가지치기 안함)

부스팅 :

이전 약한 분류기(1~6회 분할된 나무 모델)를 사용 > 이전 모델 약점을 줄이기 위해 가중치 부여

이전 모델이 오분류한 표본을 정분류하도록 만든다.

순차적으로 진행되기 때문에 오래걸리고 과적합 위험이 있음

얼리스타핑이 필요하다.

분류 :

지니 계수, 엔트로피, 카이제곱 통계량

이진 분할(재귀적학습), 다중분할(재귀적학습), 다중분할(모두범주형)

다중분할은 아무런 의미없는 ID같은 정보를 특성으로 학습하면 엔트로피가 감소!

엔트로피 감소량(정보이득) > 정보이득비 = 정보이득/내재정보

훈련 세 규모에 따라 정지규칙을 뭐로할지 정한다.

min-samples-split : 2

min-samples-node : 1

max-depth : None

사후 가지치기 : 검증셋 성능지표(정확도, 결정계수), 불필요한 가지를 잘러서 일반화하고 모델 경량화(오캄의 면도날)

비용 복잡도 : 손실 함수 + A|T|

이진 분류 : 크로스엔트로피

회귀 분류 : MSE